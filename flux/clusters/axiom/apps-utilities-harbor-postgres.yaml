apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: apps-utilities-harbor-postgres
    namespace: flux-system
spec:
    dependsOn:
        - name: infrastructure-cloudnative-pg
        - name: infrastructure-cloudnative-pg-barman
        - name: infrastructure-postgres-gateway
    interval: 10m
    sourceRef:
        kind: GitRepository
        name: flux-system
    path: ./flux/apps/utilities/harbor-postgres
    prune: true
    wait: true
    timeout: 10m

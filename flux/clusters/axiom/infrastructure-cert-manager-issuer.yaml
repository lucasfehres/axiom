apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: infrastructure-cert-manager-issuer
    namespace: flux-system
spec:
    dependsOn:
        - name: infrastructure-cert-manager
    interval: 10m
    sourceRef:
        kind: GitRepository
        name: flux-system
    path: ./flux/infrastructure/cert-manager-issuer
    prune: true
    wait: true
    timeout: 10m

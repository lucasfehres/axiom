apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: infrastructure-velero
    namespace: flux-system
spec:
    dependsOn:
        - name: infrastructure-cert-manager
    interval: 10m
    sourceRef:
        kind: GitRepository
        name: flux-system
    path: ./flux/infrastructure/opentelemetry
    prune: true
    wait: true
    timeout: 10m

apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: apps-personal-plane-postgres
    namespace: flux-system
spec:
    dependsOn:
        - name: infrastructure-cloudnative-pg
        - name: infrastructure-cloudnative-pg-barman
    interval: 10m
    sourceRef:
        kind: GitRepository
        name: flux-system
    path: ./flux/apps/personal/plane-postgres
    prune: true
    wait: true
    timeout: 10m

apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: infrastructure-loki
    namespace: flux-system
spec:
    dependsOn:
        - name: infrastructure-loki-bucket
    interval: 10m
    sourceRef:
        kind: GitRepository
        name: flux-system
    path: ./flux/infrastructure/observability/loki
    prune: true
    wait: true
    timeout: 10m

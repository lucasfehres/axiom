apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: apps-utilities-gitlab-endpoint
    namespace: flux-system
spec:
    dependsOn:
        - name: infrastructure-internal-gateway
    interval: 10m
    sourceRef:
        kind: GitRepository
        name: flux-system
    path: ./flux/apps/utilities/gitlab-endpoint
    prune: true
    wait: true
    timeout: 10m

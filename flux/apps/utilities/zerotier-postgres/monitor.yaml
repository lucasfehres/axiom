apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
    name: zerotier-postgres
    namespace: zerotier
spec:
    selector:
        matchLabels:
            cnpg.io/cluster: zerotier-postgres
    podMetricsEndpoints:
        - port: metrics

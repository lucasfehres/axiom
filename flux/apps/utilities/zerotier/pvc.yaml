apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: zerotier-data
    namespace: zerotier
spec:
    accessModes: ["ReadWriteOnce"]
    resources:
        requests:
            storage: 1Gi

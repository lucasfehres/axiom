apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: zerotier-data
    namespace: zerotier
spec:
    accessModes: ["ReadWriteOnce"]
    storageClassName: rook-ceph-block
    resources:
        requests:
            storage: 1Gi

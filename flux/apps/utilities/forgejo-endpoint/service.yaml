apiVersion: v1
kind: Service
metadata:
    name: external-forgejo
    namespace: forgejo
spec:
    ports:
        - name: http
          port: forgejo
          protocol: TCP
          targetPort: forgejo

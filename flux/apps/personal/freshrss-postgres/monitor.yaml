apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
    name: freshrss-postgres
    namespace: freshrss
spec:
    selector:
        matchLabels:
            cnpg.io/cluster: freshrss-postgres
    podMetricsEndpoints:
        - port: metrics

apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
    name: gitlab-postgres
    namespace: gitlab
spec:
    selector:
        matchLabels:
            cnpg.io/cluster: gitlab-postgres
    podMetricsEndpoints:
        - port: metrics

apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
    name: plane-postgres
    namespace: plane
spec:
    selector:
        matchLabels:
            cnpg.io/cluster: plane-postgres
    podMetricsEndpoints:
        - port: metrics
